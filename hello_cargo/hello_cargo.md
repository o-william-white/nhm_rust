```
cargo new hello_cargo
cd hello_cargo
# compile and run
cargo build
./target/debug/hello_cargo
# compile and run in one step
cargo run
# check it if compiles 
cargo check
# note there are two difference building options. Use 'cargo build --release' if finsihed testing/debugging
